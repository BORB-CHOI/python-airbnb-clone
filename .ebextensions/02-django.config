container_commands:
  01_migrate:
    command: "pipenv shell && python3 manage.py migrate --noinput"
    leader_only: true
  02_compilemessages:
    command: "pipenv shell && python3 manage.py compilemessages"
option_settings:
  aws:elasticbeanstalk:application:environment:
    DJANGO_SETTINGS_MODULE: config.settings
